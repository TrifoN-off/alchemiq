"""Domain layer - entities and value objects.

Holds your alchemiq ``Model`` definitions (``models.py``) and any pure
value objects / domain events. No I/O, no framework imports here: this is the
innermost layer that everything else depends on.
"""
